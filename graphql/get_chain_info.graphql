query GetChainInfo($network: Network!, $chain: Chain!) {
  result: GetChainInfo(network: $network, chain: $chain) {
    currentBlockNumber
    currentBlockHash
    specVersion
    transactionVersion
    metadataVersion
    metadata
  }
}